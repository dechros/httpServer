{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch Program",
            "type": "lldb",
            "request": "launch",
            "program": "${workspaceFolder}/bin/main.exe",
            "args": [],
            "cwd": "${workspaceFolder}",
            "preLaunchTask": "Build Program",
        }
    ]
}
